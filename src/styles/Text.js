import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        color: "#00ac36",
        fontSize: 60,
        fontWeight: "bold"
    },
    button: {
        color: 'white',
        textAlign: 'center',
        fontSize: 15,
        fontWeight: 'bold'
    }
})

export default styles;