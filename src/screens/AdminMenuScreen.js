import React from 'react';
import { View, Text} from 'react-native';

export default function AdminMenuScreen ({ navigation }) {
    return (
        <View>
            <Text>Menu del Administrador</Text>  
        </View>
    );
}